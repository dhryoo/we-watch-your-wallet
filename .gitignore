node_modules/
dist/
*.log
.DS_Store

# secrets — never commit (defense-in-depth on top of nested .gitignore)
.env
.env.backup
.env.local

# Laravel build / runtime (apps/web has its own .gitignore too)
/apps/web/vendor/
/apps/web/storage/
/apps/web/public/build/
/apps/web/public/hot
*.sqlite
*.sqlite-*
.phpunit.result.cache

# Claude Code local artifacts
.claude/settings.local.json

# NOT for the public repo — internal/infra/strategy (kept local only)
/deploy/
/plan.md
/done.md
/CLAUDE.md
/.claude/
