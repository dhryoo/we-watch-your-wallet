{
    "name": "watchtower",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "typecheck": "npm run typecheck --workspaces --if-present",
        "test": "npm run test --workspaces --if-present",
        "test:rpc-guard": "npm run test:rpc-guard --workspace @watchtower/detect-engine"
    },
    "devDependencies": {
        "@types/node": "^22.10.0",
        "typescript": "^5.7.3"
    }
}
